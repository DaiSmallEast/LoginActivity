<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.loginactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginFirstActivity"
            android:label="Login_first">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginSecondActivity" />
        <activity android:name=".LoginThirdActivity" />
        <activity android:name=".LoginForthActivity" />
        <activity android:name=".RegisterFirstActivity" />
        <activity android:name=".RegisterSecondActivity"></activity>
    </application>

</manifest>